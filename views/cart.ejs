<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unko Sansar</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full text-3xl text-center text-cyan-900 font-bold font-serif">My Cart</div>
    <br>
    <br>
    <div>
        <% user.cart.forEach(function(item){ %>
            
            <br>
            <div class="flex gap-30">
                <div>
                    <img class=" ml-5 w-60 h-60 shadow-lg" src="data:image/jpeg;base64,<%= item.image.toString('base64') %>" alt="">
                    <div class="flex justify-around text-white bg-cyan-800 ml-5 h-8"><div class="text-sm mt-1"><%= item.name %></div><div class="mt-1"><button class="bg-white w-6 h-6 rounded-full text-black mr-1">+</button><button class="bg-white w-6 h-6 rounded-md text-black mr-1">1</button><button class="bg-white w-6 h-6 rounded-full text-black">-</button> </div></div>
                </div>
                <div >
                    <h1 class="text-2xl font-mono text-cyan-800 font-bold">Product details</h1>
                    <br>
                    <pre>Name:           <%= item.name %></pre>
                    <br>
                    <pre>Price:          <%= item.price %></pre>
                    <br>
                    <pre>Discount:       <%= item.discount %></pre>
                    <br>
                    <hr>
                    <span class="text-yellow-800"><pre>Total :           Rs <%= item.price - item.discount %></pre> </span>
                </div>
            </div>
            
        <% }) %>
    </div>
</body>
</html>